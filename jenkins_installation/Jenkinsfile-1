@Library('shared-library') _
pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
    stage('Random Number') {
            steps {
                script {
                    def utils = new com.yourcompany.yourlibrary.Utils()
                    echo "Random Number: ${utils.getRandomNumber()}"
                }
            }
        }
    }
}
